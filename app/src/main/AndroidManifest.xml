<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fgb.ventaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VentaYa">
        <activity android:name=".UI.PantallaPublicaciones"/>
        <activity android:name=".UI.PantallaRegistro"/>
        <activity android:name=".UI.PantallaIniciarSesion"/>
        <activity android:name=".UI.PublicarVehiculos"/>
        <activity android:name=".UI.PantallaInicio">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>